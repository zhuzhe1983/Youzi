# Pixel → Atlas: composer controls and model availability

- Branch `pixel/youzi-composer-model-controls`, dependent on committed profile
  fix `e7550988` and the delivered remote-model branch. Worktree retains the
  `Youzi-profile-display-name` directory so its build cache is not relocated.
- Pixel / Local Mac. Role files and Orca absent; normal isolated Git worktree.
- Send/stop/voice share a circular component, voice rightmost; no runtime badge.
  Equal-color orb is based on effective selections, exact residency, and bounded
  read-only remote model-list checks. Unified rows distinguish resident vs selected.
- 127 tests / 20 suites passed, including bilingual/four-size/two-theme native
  rendering. Extra-large dark fixture reviewed; no user settings or inference used.
- Read-only UI probe reuses the existing endpoint and credential boundary. No public
  API or backend loading changes; only a stale readiness comment updated in Server.
- Docs: `docs/guides/youzi-composer-model-controls.md`. A list check is not generation
  capability verification. Remote services without /models cannot be verified online.
- Candidate build / live restart still pending at this commit. Main and the dirty
  memory worktree remain untouched. No release authorized.
- Next owner: Atlas integrates after review. Retain the previous candidate for
  rollback; never restart in the middle of a chat or sweep unrelated service ports.
