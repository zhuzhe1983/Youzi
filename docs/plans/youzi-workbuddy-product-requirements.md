# Youzi WorkBuddy 化功能需求草案

> 状态：需求发现稿，待产品确认；不代表已经排期或进入开发。
>
> 依据：用户提供的 7 张 WorkBuddy 界面截图，以及 Rapid-MLX Desktop 当前仓库能力盘点。
>
> 原则：截图中直接可见的行为记为“观察”；由界面推测的能力记为“推断”，开发前必须验证。

## 1. 产品结论

Youzi 不应继续以“本地模型控制台”为主要心智，而应成为一个面向非技术用户的私人智能工作台：用户只需要说清楚想完成什么，应用负责选择模型、能力和连接方式，并在需要访问外部数据或执行动作时用自然语言征求同意。

一句话定位：

> Youzi 是一位记得你的习惯、能整理你的事情、会调用合适能力并把结果收好的私人 AI 助理。

WorkBuddy 最值得借鉴的不是视觉外观本身，而是以下产品骨架：

1. 以“新任务”而不是“选择模型”作为第一入口。
2. 用场景、帮手和模板把空白输入框变成容易理解的起点。
3. 把聊天、资料和生成结果组织到持续存在的项目中。
4. 把技能和连接器做成可搜索、可解释、可启停的能力卡片。
5. 复杂能力渐进披露，日常界面不暴露底层模型和运行参数。

不建议像素级照搬 WorkBuddy。它本身仍要求用户理解“专家、技能、连接器、专家团、工作空间、权限、模型”等多个概念，市场页面信息密度也偏高。Youzi 应进一步减少名词和选择。

## 2. 目标用户与核心问题

### 2.1 主用户

- 非技术用户，主要通过自然语言表达需求。
- 不关心模型名称、量化格式、上下文长度、端口和推理参数。
- 需要确认“AI 能做什么”“会读取什么”“结果放在哪里”。
- 更容易从生活或工作场景出发，而不是从工具类型出发。

### 2.2 当前体验的主要学习成本

1. 先理解和管理模型，才能开始一件普通任务。
2. Chat、Images、Audio、Video 按技术能力分区，用户需要先判断该去哪个页面。
3. Tools、Connectors、System Prompt、Memory、Performance 等设置直接暴露实现概念。
4. 会话记录能保存，但缺少围绕同一件事情持续积累资料和成果的“项目”心智。
5. 空白对话依赖用户自己写出好提示词，没有足够的任务起点和结果示例。

### 2.3 产品目标

- 首次打开后，用户无需进入设置即可开始第一个有效任务。
- 大部分日常任务无需看见或选择模型。
- 用户能够理解当前任务引用了哪些资料、会访问哪些服务。
- 用户能快速找回某件事情及其历史对话、资料和生成结果。
- 高级用户仍可进入高级设置管理模型和连接器，但不影响日常路径。

### 2.4 非目标

- 第一阶段不建设开放式第三方专家市场。
- 第一阶段不建设多人团队、组织权限和云端协作。
- 不因模仿截图而虚构当前不存在的后端能力。
- 不取消本地优先、权限确认和高级模型管理能力。
- 不把所有媒体能力强行塞进同一个低质量的通用聊天界面。

## 3. 截图功能拆解

| 截图 | 页面 | 直接观察到的功能 | 需要验证的推断 |
| --- | --- | --- | --- |
| 1 | 首页 / 新任务 | 固定侧栏；任务历史；空间列表；日常办公/代码开发模式；场景快捷入口；大输入框；工作空间选择；权限选择；自动模型；发送按钮 | 空间是否自动注入文件和指令；“Auto”如何选择模型；场景入口是提示词模板还是完整工作流 |
| 2 | 专家目录 | 专家/技能/连接器顶栏；搜索；“我的专家”；精选场景；专家与专家团切换；分类；综合/最热/最新排序；专家卡片、作者、简介、标签 | 专家是否包含固定提示词、工具组合、知识库和模型策略；热度数据来源 |
| 3 | 专家团目录 | 与专家目录共用筛选和卡片系统；团队卡片带成员/组织、简介和标签 | 专家团是否支持多智能体协作，还是若干角色的预设编排 |
| 4 | 技能目录 | 精选技能；搜索；“我安装的”；添加技能；推荐/套件切换；市场来源切换；技能卡片与安装按钮 | 技能包格式、安装安全检查、版本更新、依赖和卸载机制 |
| 5 | 连接器目录 | 搜索；自定义连接器；品牌卡片；能力简介；添加按钮；已连接状态 | OAuth、CLI、MCP 或其他协议的具体接入方式；凭证保存和权限范围 |
| 6 | 项目任务 | 左侧项目与子任务；顶部面包屑、搜索、同步、协作状态、成员和布局控制；中间对话；多角色回复；生成文件链接；右侧文档预览；输入框可引用资料/待办/技能；模型选择 | 项目资产的持久化方式；多人实时协作；产物编辑能力；多角色调度机制 |
| 7 | 灵感模板 | 灵感入口；分类；收藏；搜索；大幅结果预览；模板名称、类型、说明、官方来源和收藏按钮 | 点击模板后是复制项目、生成预填任务，还是直接运行工作流 |

图 7 与前六张图在主题、语言和导航结构上存在明显差异，可能来自不同版本或不同端，因此只能证明“灵感模板”这一产品模式，不能据此认定所有页面已经采用同一套实现。

### 3.1 截图体现的整体信息架构

```text
工作台
├── 新任务
├── 任务历史
├── 项目 / 空间
├── 能力中心
│   ├── 专家
│   ├── 专家团
│   ├── 技能
│   └── 连接器
├── 自动化
└── 灵感 / 模板
```

### 3.2 降低学习成本的有效机制

- 用“新任务”作为全局主操作。
- 保持侧栏位置稳定，让历史和项目随时可见。
- 用场景卡片和模板提供可识别的任务起点。
- 卡片同时展示名称、用途、作者/来源和标签，减少试错。
- 搜索、分类、收藏、已安装等状态让能力可管理。
- 生成产物在右侧独立预览，避免结果淹没在长对话中。
- 面包屑持续说明用户当前在哪个项目和任务中。
- 连接器展示品牌和连接状态，减少抽象配置感。

### 3.3 WorkBuddy 自身仍存在的认知负担

- “专家、技能、连接器”同时出现，普通用户不一定理解三者边界。
- 专家与专家团是第二层抽象，对个人单用户产品价值有限。
- 首页输入框同时放置工作空间、权限和模型，首屏决策仍偏多。
- 能力市场卡片数量很大，容易从“不会开始”变成“不会选择”。
- 左侧导航与顶部页签形成两层主导航，需要学习页面层级。
- 部分截图中文、英文和技术名词混用，降低一致性。

## 4. Youzi 的推荐信息架构

### 4.1 日常模式

```text
Youzi
├── 新任务
├── 我的事情
│   ├── 最近任务
│   └── 项目
├── 帮手中心
│   ├── 帮手
│   ├── 会做的事
│   └── 已连接应用
├── 定时帮我（后续）
└── 灵感
```

### 4.2 高级设置

模型管理、运行状态、性能参数、MCP、自定义连接器和诊断仍然保留，但统一收进“设置 → 高级”。日常模式只显示：

- 自动选择是否可用。
- 当前是否正在准备。
- 失败后用户能采取的下一步。
- 某项能力是否需要联网或访问外部应用。

### 4.3 名词映射

| WorkBuddy / 当前 Rapid 名词 | Youzi 日常界面建议 | 说明 |
| --- | --- | --- |
| Chat / New Chat | 新任务 | 用户的目标不是开始聊天，而是完成一件事 |
| Conversation | 任务记录 | 保留对话形态，但用任务心智组织 |
| Folder | 项目或分组 | 普通分组与真正项目需在数据模型上区分 |
| Workspace / Space | 项目 | 包含资料、偏好、任务和成果的持续上下文 |
| Expert | 帮手 | 一套角色、语气、工作步骤和推荐能力 |
| Expert Team | 帮手组合 | 非 MVP；只有多角色确实提升结果时才出现 |
| Skill / Tool | 会做的事 | 用户看到用途；技术来源放在详情中 |
| Connector / MCP | 已连接应用 | 例如日历、文件、Notion；MCP 只在高级详情中出现 |
| Artifact | 成果 | 文档、表格、图片、音频、视频或导出文件 |
| Inspiration | 灵感模板 | 可预览、收藏并一键开始的任务模板 |
| Automation | 定时帮我 | 用自然语言表达触发时间、动作和通知方式 |
| Model picker | 自动 | 默认隐藏；高级菜单可覆盖 |
| Permissions | 访问确认 | 明确说明“要访问什么、为什么、允许多久” |

## 5. 核心对象定义

### 5.1 任务

一次明确用户意图及其完整过程，包含：

- 用户需求与补充说明。
- 使用的项目资料。
- 调用过的帮手、能力和已连接应用。
- 权限确认记录。
- 对话过程与最终成果。
- 状态：进行中、等待确认、已完成、失败。

### 5.2 项目

围绕一件持续事项保存上下文，包含：

- 项目说明和偏好。
- 文件、图片和其他资料。
- 相关任务。
- 已生成成果。
- 默认可用的帮手、能力和连接应用。

项目不是仅用于整理会话的文件夹。若第一阶段复用现有文件夹能力，界面不得提前承诺资料自动注入等尚未实现的行为。

### 5.3 帮手

面向用户的能力预设，可能由以下内容组成：

- 名称和适用场景。
- 角色说明与表达风格。
- 推荐步骤或提示结构。
- 推荐启用的能力。
- 预期输出类型。

帮手不是一个必须单独购买或理解的“模型”。同一帮手可以使用自动选择的本地模型。

### 5.4 会做的事

一个可被 Youzi 调用的原子能力，例如查天气、搜索网页、读取某个连接应用或生成图片。详情必须说明：

- 能完成什么。
- 会读取或发送什么数据。
- 在本机完成还是需要联网。
- 是否需要首次确认。
- 当前是否可用。

### 5.5 已连接应用

与外部服务或本地程序的连接。日常界面显示品牌、用途和状态；协议、命令、环境变量等仅在高级设置显示。

### 5.6 成果

任务产生的可复用结果。成果应拥有稳定标题、类型、预览、保存位置和导出动作，而不是只作为聊天消息中的一段文本存在。

### 5.7 灵感模板

可预览的任务起点，包含示例结果、预填需求、推荐帮手/能力及所需资料。使用模板时先创建草稿，未经用户确认不直接执行外部动作。

### 5.8 定时帮我

由触发条件、任务动作、权限范围、通知和运行记录组成的自动化。由于涉及后台运行、失败恢复和长期授权，不进入 MVP。

## 6. 功能需求

### FR-01：任务优先首页

- 首屏主标题使用自然语言，例如“今天想让我帮你做什么？”
- 唯一主操作是输入并开始任务。
- 默认只有一个稳定的“柚子助手”人格；专业能力由系统自动匹配，不要求用户先挑角色。
- 提供 4–6 个可配置的常用场景，不展示模型列表。
- 最近任务和项目在侧栏可直接恢复。
- 图片、语音、视频和文档能力通过场景入口或输入附件进入，而不是要求用户先选择技术页面。
- 高级用户可在次级菜单覆盖自动模型，但该控件不占据首屏视觉层级。

### FR-02：渐进式任务输入框

- 默认只显示输入、添加资料和发送。
- 选择项目后，以一行摘要显示将引用的项目资料。
- 即将访问外部应用时，在执行前显示具体访问说明。
- 将“默认权限”改成可理解的状态，例如“访问外部应用前先问我”。
- 模型准备、下载和启动合并成任务进度，避免独立的 Start/Stop 心智。
- 错误文案说明发生了什么以及下一步，不显示端口、进程或原始运行时错误。

### FR-03：任务与项目侧栏

- 侧栏固定展示新任务、最近任务和项目。
- 最近任务按今天、本周、更早分组，并支持搜索、重命名、置顶、归档和移动。
- 项目可以展开查看相关任务。
- 空项目有清晰的“添加资料”或“开始第一个任务”入口。
- 项目与普通历史分组在视觉和语义上必须区分。

### FR-04：帮手中心

- 统一容纳“帮手、会做的事、已连接应用”三个页签。
- 首页只推荐少量经过确认的卡片，不展示无限市场流。
- 每张卡片显示名称、用途、来源、数据边界和可用状态。
- 支持搜索、分类、收藏/常用和仅看已启用。
- 卡片详情使用示例任务解释能力，不以配置字段作为主要内容。
- “添加自定义能力/连接”放入高级入口。

### FR-05：帮手

- 用户可以选择一个帮手开始任务，也可以让 Youzi 自动选择。
- 首批帮手应来自用户真实高频场景，而不是通用职业大全。
- 帮手详情展示“适合做什么、会怎么帮、会用到什么”。
- 帮手只改变任务策略和结果风格，不绕过权限确认。
- 第一阶段不支持公开发布、评分、热度和第三方交易。

### FR-06：会做的事

- 将当前内置工具和未来技能呈现为用途卡片。
- 启用前显示联网、文件或第三方访问范围。
- 支持启用、停用和查看最近使用情况。
- 有依赖或配置缺失时，卡片显示可恢复状态和单一下一步。
- 不把提示词模板、执行工具和连接应用混为一个无法解释的“技能”。

### FR-07：已连接应用

- 使用品牌和用途卡片展示可连接对象。
- 状态至少包含：未连接、连接中、已连接、需要处理、已停用。
- 连接向导逐步说明将获得的权限，并允许取消和返回。
- 用户可以查看、暂停、重新授权和断开连接。
- 自定义 MCP/命令/URL 编辑器保留在高级设置。
- 任何工具调用仍需遵循当前的逐项授权或明确的长期授权策略。

### FR-08：项目工作台

- 顶部始终显示项目与任务层级。
- 中间区域负责对话和任务进度。
- 当存在文档、表格、图片等成果时，可打开右侧成果预览。
- 用户能从任务中引用项目资料；普通用户入口使用“引用资料”，高级入口可保留 `@`。
- 成果可命名、保存到项目、在 Finder 中显示或导出。
- 单用户版本不显示无意义的协作人数和在线状态。

### FR-09：灵感模板

- 按日常办公、生活安排、资料整理、内容创作等用户语言分类。
- 模板卡片以示例成果预览为主，而不是只有提示词标题。
- 支持搜索、收藏和“用这个开始”。
- 启用模板后创建可编辑任务草稿，并提示还需要哪些资料。
- 第一阶段模板由本地内置或受控清单提供，不建设开放发布平台。

### FR-10：定时帮我（后续）

- 用户以自然语言创建，例如“每周五整理本周完成的事情”。
- 创建前确认时间、资料范围、允许的外部动作和通知方式。
- 展示下一次运行、最近结果、暂停和立即运行。
- 每次运行有可审计记录，失败时提供重试或修改入口。
- 长期授权必须可随时撤销。

### FR-11：普通模式与高级设置

- 普通模式隐藏模型别名、量化、MTP、KV cache、端口和服务进程。
- 普通模式默认自动选择兼容模型，并在切换前保护当前任务状态。
- 高级设置保留现有模型管理、性能、实验功能、连接器和诊断能力。
- 从普通界面的故障提示可深链到对应高级设置，但不要求用户自己定位。

### FR-12：隐私与信任

- 任何会离开本机的数据流都必须在首次使用前说明。
- 权限提示必须回答：访问什么、为什么、执行一次还是以后都允许。
- “本机完成”和“需要联网”除颜色外还要有文字或图标说明。
- 断开连接、撤销授权和清除项目资料必须容易找到。
- 自动化、连接器和帮手都不得绕过底层工具授权。

## 7. 关键用户流程

### 7.1 首次完成任务

1. 用户打开 Youzi。
2. 首页展示一句邀请语和少量常用场景。
3. 用户输入需求或选择场景。
4. Youzi 自动选择本地模型并准备运行。
5. 如需联网或连接应用，执行前用自然语言确认。
6. 任务完成后突出显示成果，并询问是否保存到项目。

验收重点：整个流程不要求用户进入模型管理或理解技能/连接器差异。

### 7.2 从项目继续一件事

1. 用户在侧栏打开项目。
2. 页面展示最近任务、资料和成果。
3. 用户选择已有任务继续，或新建任务并引用项目资料。
4. 生成的新成果自动关联当前项目，但保存到外部位置前仍需用户确认。

### 7.3 连接一个应用

1. 用户在帮手中心搜索熟悉的应用名称。
2. 卡片先说明用途和数据范围。
3. 用户点击连接，按步骤完成授权。
4. 完成后显示“已连接”及可执行的任务示例。
5. 第一次实际调用敏感动作时再次进行执行级确认。

### 7.4 使用灵感模板

1. 用户浏览成果预览并选择模板。
2. Youzi 创建预填任务草稿。
3. 界面列出还需要的资料或选择。
4. 用户确认后执行。
5. 结果作为成果保存，可继续修改。

## 8. 与当前 Rapid-MLX Desktop 的能力映射

### 8.1 可以复用

- 本地对话、流式回复和会话持久化。
- 会话搜索、重命名、置顶、归档、文件夹和拖放整理。
- 文件与图片附件。
- 自定义指令和本地记忆。
- 网页搜索、浏览和天气等内置工具。
- MCP 连接器、逐工具开关和调用授权。
- 图片、音频和实验性视频能力。
- 本地模型下载、启动、自动恢复和资源保护。
- 会话导入导出与诊断基础设施。

### 8.2 需要重塑交互

- 将 Chat / Images / Audio / Video 的技术导航改成任务和场景入口。
- 将模型启动、下载和选择收敛到“自动”，把详细状态放到高级设置。
- 将 Tools / Connectors 改写成“会做的事 / 已连接应用”的用户语言。
- 将现有文件夹保留为整理能力，避免直接冒充拥有资料上下文的完整项目。
- 将工具授权改成更具体、更短的自然语言说明。

### 8.3 当前缺少，需要新增产品能力

- 真正的项目上下文和项目资产模型。
- 帮手定义、详情和受控目录。
- 技能/能力元数据、来源、数据边界和安装状态目录。
- 面向品牌应用的连接器发现与连接向导。
- 独立成果对象和右侧预览工作台。
- 灵感模板及示例成果预览。
- 自动化调度、运行记录和长期授权管理。
- 专家团、多角色协作及多人项目能力。

以上新增项涉及数据模型、存储、权限或后台运行时，应在开发前由 Atlas 做架构确认。

## 9. 建议分期

### MVP：先消除学习门槛

- 任务优先首页与新的侧栏信息架构。
- 自动模型作为默认路径，隐藏运行时细节。
- 常用场景快捷入口。
- 最近任务、现有文件夹和搜索能力的友好呈现。
- “会做的事 / 已连接应用”简化页，先映射现有内置工具和 MCP。
- 普通语言权限确认和联网/本机标识。
- 高级设置入口，完整保留现有能力。

### P1：形成持续工作台

- 真正的项目与项目资料。
- 少量内置帮手和帮手详情。
- 成果对象与右侧预览。
- 灵感模板、收藏和从模板开始。
- 受控的连接器目录与连接向导。
- 常用能力收藏和最近使用。

### P2：在真实需求验证后扩展

- 定时帮我与后台运行记录。
- 帮手组合或多角色协作。
- 第三方技能包、版本和依赖管理。
- 开放市场、热度、评分和发布。
- 多人协作、成员和项目权限。
- 跨设备同步。

## 10. MVP 验收标准

- 新用户能在 60 秒内开始第一个任务，不进入设置。
- 新用户能在 2 分钟内得到第一个有用成果。
- 测试任务中至少 80% 无需手动选择模型。
- 用户能在 5 秒内从侧栏找回一个最近任务。
- 用户能用自己的话解释“会做的事”和“已连接应用”的区别。
- 任何外部访问在执行前都能看到明确的对象、目的和授权时长。
- 没有模型别名、端口、量化或 MCP 字样进入普通模式主流程。
- 键盘、VoiceOver、动态字体和减少动态效果保持可用。
- 普通模式的失败状态都提供一个明确的恢复动作。
- 一级导航不超过 5 项，主输入区默认常驻控件不超过 3 类。

## 11. 视觉与交互方向

- 延续 macOS 原生结构、稳定侧栏和清晰选中状态。
- 使用 Youzi 柚子 Logo 的暖色作为克制的强调色，不采用高饱和市场风格。
- 卡片用于“可选择的能力或模板”，普通设置项不滥用卡片。
- 每页只保留一个主操作；搜索是目录页主入口。
- 正文和次要文字在深浅主题下均满足可读对比。
- 图标统一使用系统或同一套矢量图标，不使用 Emoji 充当结构图标。
- 点击目标至少 44×44pt；键盘焦点、禁用、加载和失败状态清晰。
- 过渡时间控制在 150–300ms，并支持 Reduce Motion。
- 大目录使用懒加载或虚拟列表；异步内容预留尺寸，避免页面跳动。

## 12. 待产品确认的问题

1. Youzi 最常见的五类真实任务是什么？这会决定首页场景和首批帮手，而不是从市场模板倒推。
2. “项目”是否需要自动引用其中所有资料，还是只做任务分组并由用户逐次选择资料？
3. 是否需要任何开放市场，还是只由家庭管理员维护一份精选能力清单？
4. Youzi 可以把哪些数据发送到互联网服务，哪些必须永远只在本机处理？
5. 是否真的需要定时自动执行？若需要，首个自动化场景是什么？
6. 模型和连接器的高级设置由同一个用户维护，还是需要一个更隐蔽的“管理员模式”？
7. 首批成果类型优先级是什么：文档、表格、图片、语音还是视频？
8. 首页是否需要“日常 / 工作”两个模式，还是由项目和场景自动区分即可？
9. Youzi 最常处理哪些文件：Word、PDF、Excel、图片、PPT 还是网页？
10. 生成结果需要在应用内编辑，还是预览后导出即可？
11. 她更偏好亲切陪伴还是简洁专业的语气？回答长度是否需要固定偏好？
12. 默认使用深色、浅色还是跟随系统？
13. 当前 Chat、Images、Audio、Video、Launch 中有哪些页面必须保留独立入口？
14. 第一版只服务 macOS，还是需要同时考虑 Web 或其他设备？
15. 第一版最重要的排序是什么：更易用、更丰富，还是更有私人温度？

## 13. 开发前门槛

在开始统一开发前，至少完成以下确认：

- 确认前五类高频任务和首页快捷入口。
- 确认 MVP/P1/P2 边界。
- 确认“项目、帮手、会做的事、连接应用、成果”的定义。
- 确认本机/联网数据边界和权限文案原则。
- 由 Atlas 评审项目、成果、自动化等新增数据模型。
- 为首页、任务页、帮手中心和项目页分别产出线框图。
- 用非技术用户完成 5 个代表任务的可用性走查，再进入实现。
